agent_model:
  name: "Qwen/Qwen3-1.7B"
  type: "qwen"
  temperature: 0.6
  top_p: 0.6
  top_k: null
  max_length: 2048
  torch_dtype: "auto"

agents: null

critic_model: null

critics: null

dataset:
  name: "trl-lib/tldr"
  type: "tldr"
  train_split: "train[:1000]"
  eval_split: "test[:1000]"

tokenizer:
  padding_side: "left"

output:
  base_dir: output_grpo_tldr
  verbose: false
  save_final_model: true
  save_path: output_grpo_tldr

grpo:
  parallel_training: none
  num_turns: 1
  num_train_epochs: 1
  agent_learning_rate: 5.0e-6
  logging_steps: 400
  num_generations: 4
  joint_mode: aligned
  max_new_tokens: 256
  rollout_buffer_size: 2
  train_batch_size: 2
  advantage_normalization: true
  eval_interval: 4
  eval_num_samples: 4
  eval_batch_size: 1

reward_processor:
  enabled: true
  scale_factor: 1.0
  shift: 0.0

wandb:
  project: "comlrl"
  entity: "OpenMLRL"
  name: "grpo_tldr"
  tags: ["grpo", "tldr", "single-agent"]
